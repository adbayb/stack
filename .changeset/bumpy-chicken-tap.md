---
"@adbayb/stack": minor
---

The release command flags have been updated:

- Add a `snapshot` flag.
- Rename `log` flag to `changelog`.
- Rename `empty-log` flag to `empty-changelog`.
- Rename `increment` flag to `bump`.
