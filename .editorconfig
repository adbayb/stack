root = true

[*]
# editorconfig to configure ide display settings (like indent_size):
# We create one mainly for github purposes to display tab indent with a size of
# 4 instead of the default browser one (e.g. 8)
indent_size = 4